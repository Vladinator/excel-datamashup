import { ReadableStream } from 'web-streams-polyfill';
/**
 * This ReadableStream is different than the Node ReadableStream.
 * This module supports both Node and browser environment.
 */
export { ReadableStream };
